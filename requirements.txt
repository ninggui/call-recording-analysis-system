# 通话录音批量处理系统 - Python 依赖

# 核心依赖 lark-cli（飞书命令行工具，npm 包）：
#   npm install -g @larksuite/cli
#
# Python 脚本依赖（按实际脚本使用情况补充，以下为最小集）：
# 如脚本使用 requests 调用飞书 API：
requests>=2.28
# 如脚本需要读取环境变量配置：
python-dotenv>=1.0
